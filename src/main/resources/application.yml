spring:
  datasource:
    #mysql local
    url: jdbc:mysql://localhost/vollmed_api
    username: newuser
    password: password

    #postgres prod

  jpa:
    show-sql: true

    properties:
      hibernate:
        format_sql: true
