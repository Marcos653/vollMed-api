spring:
  datasource:
    #mysql local
    url: jdbc:mysql://${MYSQLHOST}:${MYSQLPORT}}/${MYSQLDATABASE}
    username: ${MYSQLUSER}
    password: ${MYSQLPASSWORD}

  jpa:
    show-sql: true

    properties:
      hibernate:
        format_sql: true
